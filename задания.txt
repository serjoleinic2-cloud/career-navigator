Не интегрируй AppShell в App.tsx пока.

Сейчас нужно только создать UI Foundation как независимый слой.

Сделай следующее:

1. Создай компоненты:
- AppShell
- TopBar
- BottomNavigation
- GlassCard
- PrimaryButton
- IconButton
- ProgressRing

2. Создай:
- theme.css
- layout.css
- animations.css

3. AppShell должен принимать через props:
- title
- showBack
- onBack
- children
- activeTab
- onTabChange

4. BottomNavigation не должна знать ничего о runtime, navigation, Journey или React Router. Только callback onTabChange(tabId).

5. TopBar также не должна знать о runtime. Только отображение title и callbacks.

6. Никакой логики приложения не переносить.

7. Никакие существующие экраны не изменять.

8. App.tsx не трогать.

9. JourneyScreen не трогать.

10. Это инфраструктурный патч. После него компоненты должны существовать, но ещё нигде не использоваться.

После завершения предоставить IMPLEMENTATION REPORT и список созданных файлов.
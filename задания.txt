Create a React + TypeScript component called WorldMap for a gamified Career Navigator app.

This is NOT a dashboard or navigation screen.

It is a game world representation of user career progress.

Requirements:

1. Layout:
- Vertical path of nodes (top = future, bottom = start)
- Active node centered in view
- Future nodes blurred/faded
- Completed nodes below active

2. Nodes:
- 4 states: completed, active, locked, future
- Visual differences must be strong (glow, blur, opacity)

3. Interaction:
- Tap node → opens MissionScreen
- Vertical swipe → scroll camera along path
- Long press → optional details

4. World behavior:
- World reacts to JourneyRuntimeState updates
- activeNodeId controls camera focus
- nodeStates control visuals

5. Zones:
- START (plains)
- LEARNING (forest)
- PRACTICE (mountains)
- INTERVIEW (city)
- OFFER (castle)

6. Style:
- Monument Valley inspired minimal 3D feel
- soft lighting, fog, depth layers
- minimal UI overlay

7. Animation:
- smooth camera movement (600–900ms)
- node activation pulse
- path glow on progress

Output:
- full React component
- TypeScript types
- CSS modules or styled system

PROMPT ДЛЯ OPENCODE
Integrate WorldMap as the main screen of Career Navigator.

Requirements:

1. Replace existing JourneyScreen as primary view.
2. Ensure WorldMap reads from JourneyRuntimeState:
   - activeNodeId
   - nodeStates
3. Implement camera focus system based on activeNodeId.
4. Connect node click → MissionScreen navigation.
5. Ensure state updates after Mission completion reflect immediately on map.
6. Add smooth transitions between WorldMap and MissionScreen.
7. Maintain offline-first architecture.
8. Remove any legacy dashboard navigation patterns.

Constraints:
- NO list-based UI
- NO traditional dashboard components
- MUST follow game-world paradigm

Create a modular profession package for a gamified Career Navigator app.

Profession: Software Engineer

This is NOT UI-only. It is a full game module.

Structure:

1. world-map definition:
- nodes for progression:
  fundamentals → algorithms → system design → interviews → offer

2. missions:
- 10–15 tasks
- each task is a small learning/action step

3. interview system:
- list of technical + behavioral questions
- structured as boss fight stage

4. progression system:
- skill growth per node completion
- confidence score impact

5. playbook:
- rules of software engineering career progression

6. style:
- must match Monument Valley inspired world system

Output:
- JSON or TypeScript module
- structured, not UI components
- reusable by core engine
PROMPT ДЛЯ OPENCODE
Implement profession module system in Career Navigator.

Requirements:

1. Create /professions directory structure.
2. Implement loader:
   loadProfession(professionId)
3. Ensure WorldMap uses profession module as data source.
4. Replace any hardcoded journey data with profession-driven data.
5. Active profession must control:
   - nodes
   - missions
   - interview content
6. Ensure runtime supports switching professions.
7. Maintain separation between core engine and profession modules.

Constraints:
- NO hardcoded UI flows
- ALL progression must come from profession module
- system must be extensible for future professions
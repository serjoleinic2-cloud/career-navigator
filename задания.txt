Create a React + TypeScript screen called MissionScreen for a gamified Career Navigation app.

The screen is NOT a form or task list.

It is a single-focus game level experience.

Requirements:

1. Layout:
- Header with Day, Chapter, CareerScore, ConfidenceScore
- Center Mission Card (single active task only)
- Bottom primary action button

2. Mission Card:
- displays one task at a time
- supports 3 types:
  - text input task
  - action checklist task
  - reflection rating task

3. Behavior:
- only one active task
- completion triggers callback submitTask()
- updates runtime state

4. Visual style:
- minimal Apple-like UI
- soft shadows
- floating central card
- large whitespace
- no dashboards, no lists

5. Animations:
- screen enter fade + slide up
- task completion pulse
- button press micro interaction

6. Integration:
- uses JourneyRuntimeState
- calls applyTaskResultToRuntime()
- emits UI_REFRESH event

Output:
- full React component
- TypeScript types
- basic CSS modules styling
⚙️ 11. PROMPT FOR OPENCODE (INTEGRATION TASK)
Integrate MissionScreen into existing Career Navigator project.

Tasks:

1. Add MissionScreen as main task execution screen.
2. Route from World Map node click → MissionScreen.
3. Pass active node task from JourneyRuntimeState.activeNodeId.
4. Ensure submitTask() updates runtime state correctly.
5. Trigger UI_REFRESH event after task completion.
6. Fix any state sync issues between nodeStates and activeTask.
7. Ensure only one active task is visible at a time.
8. Remove any legacy multi-task UI if exists.

Constraints:
- Must follow existing architecture in ARCHITECTURE_SNAPSHOT
- Must NOT introduce new screens outside agreed list
- Must keep offline-first behavior
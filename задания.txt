РЕАЛЬНОЕ СОСТОЯНИЕ (не assumptions):
Table
Файл	Что проверил	Результат
skill_engine.ts	Есть ли on('MISSION_SUBMIT')? Есть ли emit('MISSION_RESULT')? Есть ли guards?	НЕТ — ни одного из трёх
system_event_bus.ts	Есть ли duplicate protection?	НЕТ — emit вызывает все listeners без dedup
world_renderer.tsx	Есть ли cached state?	ЧИСТО — только consume + render
MissionScreen.tsx	Прямой вызов или event?	ПРЯМОЙ ВЫЗОВ applyMissionResult
App.tsx	Правильный routing?	ЧИСТО
5 КРИТИЧЕСКИХ БАГОВ:
MissionScreen — прямой вызов applyMissionResult вместо emit('MISSION_SUBMIT')
skill_engine — нет обработчика on('MISSION_SUBMIT')
skill_engine — нет emit('MISSION_RESULT') после обработки
MissionScreen — нет listener на MISSION_RESULT
skill_engine — нет guard isProcessingMission (double submit возможен)
Event flow НЕ работает как задумано. Сейчас:
plain
MissionScreen → applyMissionResult() [прямой вызов] → skill_engine → UI_REFRESH
Должно быть:
plain
MissionScreen → emit('MISSION_SUBMIT') → skill_engine → emit('MISSION_RESULT') → MissionScreen